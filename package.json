{
  "name": "map-input-action",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm ci && esbuild index.js --bundle --platform=node --outfile=dist/index.js",
    "bundle": "esbuild index.js --bundle --platform=node --outfile=dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.6"
  },
  "devDependencies": {
    "esbuild": "^0.12.2"
  }
}
